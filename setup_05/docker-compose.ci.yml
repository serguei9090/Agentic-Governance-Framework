version: '3.8'

services:
  ci-runner:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./:/app
    command: /bin/sh -c "npm install && npm run lint && npm run test && npm run build"
