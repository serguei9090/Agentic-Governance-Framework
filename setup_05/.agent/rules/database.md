# Database Guidelines

**Purpose:** Connection pooling and ORM best practices.

*   **Database:** ChromaDB (Vector), SQLite (Dev), Postgres (Prod).
*   **ORM:** SQLAlchemy or Pydantic models.
*   **Pooling:** Use connection pooling for SQL databases to handle concurrent requests.
